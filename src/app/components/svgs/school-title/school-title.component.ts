import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-school-title',
  templateUrl: './school-title.component.html',
  styleUrls: ['./school-title.component.scss'],
})
export class SchoolTitleComponent implements OnInit {

  constructor() { }

  ngOnInit() {
    // const paths = document.querySelectorAll('.login-title>path') as any;
    // paths.forEach(path => {
    //   console.log(path.getTotalLength());
    // });

    // console.log(paths.length);
  }

}
